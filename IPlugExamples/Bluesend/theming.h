#pragma once

#include "IPlugStructs.h"

// Define some custom colors
const IColor COLOR_LIGHT_BLUE(255, 56, 164, 208);
const IColor TITLE_COLOR = COLOR_WHITE;
const IColor LABEL_COLOR(240, 255, 255, 255);
const IColor ACCENT_TEXT_COLOR = COLOR_LIGHT_BLUE;